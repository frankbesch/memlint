# memvet as a GitHub Action. Downloads a released binary for the runner,
# verifies it against the checksums goreleaser publishes, and runs check with
# annotations on. The binary is the same one `brew` and `go install` give you;
# this file only fetches it.
name: memvet
description: Integrity checks for file-based AI agent state (memory, instructions, decisions, contracts)
author: Frank Besch
branding:
  icon: check-square
  color: green

inputs:
  path:
    description: Directory to check (where .memvet.toml lives, or where one would be inferred)
    required: false
    default: "."
  strict:
    description: Treat YELLOW findings as failures (also fails a run that had to infer its config)
    required: false
    default: "false"
  base:
    description: Git ref to compare [append_only] files against (e.g. the pull request base sha)
    required: false
    default: ""
  version:
    description: Release to run, e.g. v0.12.0, or "latest"
    required: false
    default: "latest"

runs:
  using: composite
  steps:
    - name: Download memvet
      shell: bash
      env:
        GH_TOKEN: ${{ github.token }}
        VERSION: ${{ inputs.version }}
      run: |
        set -euo pipefail
        case "$(uname -s)" in
          Linux)  os=linux ;;
          Darwin) os=darwin ;;
          *) echo "memvet action: unsupported OS $(uname -s)"; exit 2 ;;
        esac
        case "$(uname -m)" in
          x86_64)        arch=amd64 ;;
          aarch64|arm64) arch=arm64 ;;
          *) echo "memvet action: unsupported arch $(uname -m)"; exit 2 ;;
        esac
        if [ "$VERSION" = "latest" ]; then
          VERSION=$(gh release view --repo frankbesch/memvet --json tagName --jq .tagName)
        fi
        # goreleaser's default archive name: memvet_0.12.0_linux_amd64.tar.gz
        archive="memvet_${VERSION#v}_${os}_${arch}.tar.gz"
        dir="$RUNNER_TEMP/memvet"
        mkdir -p "$dir"
        gh release download "$VERSION" --repo frankbesch/memvet --pattern "$archive" --pattern checksums.txt --dir "$dir" --clobber
        (cd "$dir" && grep " $archive\$" checksums.txt | sha256sum -c -)
        tar -xzf "$dir/$archive" -C "$dir" memvet
        echo "$dir" >> "$GITHUB_PATH"
        "$dir/memvet" --version

    - name: memvet check
      shell: bash
      env:
        STRICT: ${{ inputs.strict }}
        BASE: ${{ inputs.base }}
        TARGET: ${{ inputs.path }}
      run: |
        set -euo pipefail
        args=()
        [ "$STRICT" = "true" ] && args+=(--strict)
        [ -n "$BASE" ] && args+=(--base "$BASE")
        memvet check --format github ${args[@]+"${args[@]}"} "$TARGET"
