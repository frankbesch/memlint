# The minimal recipe, on a repo where two things went wrong.
# memvet check examples/broken exits 1 with 1 red, 1 yellow.

[pointers]
files = ["MEMORY.md"]
roots = ["memory"]

[tokens]
watch = ["MEMORY.md", "memory/*.md"]
budget = 400
