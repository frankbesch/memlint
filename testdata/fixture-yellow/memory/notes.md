# Notes

This note is over the deliberately tiny 20-token budget declared for this
fixture, which makes it the second of two advisory findings here. Neither one
is a violated invariant, so the run stays green unless --strict is passed.
