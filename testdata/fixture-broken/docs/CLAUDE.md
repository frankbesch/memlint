# Project contract

This file is mirrored into `docs/` so the docs site serves the same contract
the agent reads. THIS COPY HAS DRIFTED and no longer matches the root file.

## Rules

- Never edit generated files by hand.
- Record every decision in the log.
